describe('Object Type Test', () => {
    it('should use object type', () => {
        const bahan : {id: string, name: string}={
            id:"123123",
                name:"Katun"
        }
        console.info(bahan)
    });
    
});
